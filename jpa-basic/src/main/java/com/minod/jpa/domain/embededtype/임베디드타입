JPA에서 임베디드 타입을 굳이 구분하는게 중요하다.
어노테이션 붙여 사용하는 Entity 객체도 사실 임베디드 타입 이라고한다.

사용자가 정의한 특정 타입으로써 기본형 값을 묶어 사용하는 타입이 될 수 있다고 한다.
객체를 왜 이렇게 타입이라고 말하는가?
-- • 새로운 값 타입을 직접 정의할 수 있음
   • JPA는 임베디드 타입(embedded type)이라 함
   • 주로 기본 값 타입을 모아서 만들어서 복합 값 타입이라고도 함
   • int, String과 같은 값 타입을 모아서 만듬.

이렇다보니 JPA의 Entity객체도 임베디드 타입이다 라고 끼워 맞추는 것이다.


